function submitRole() {
    const selectedRole = document.querySelector('input[name="role"]:checked').value;
    alert("Você escolheu: " + selectedRole);
    // Aqui você pode adicionar o código para redirecionar ou processar a escolha do usuário
}
